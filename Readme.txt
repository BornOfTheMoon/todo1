Запуск

server: npm run start:dev
client: npm start



Docker

docker-compose up



БД

необходимые переменные для подключения к PostgreSQL (в частности пароль) 
находятся в файле .env



Проблема

когда открывается страница со всеми тасками, изначально они не отображаются, 
необходимо повторно перейти на неё же, кликнув на лого (нажатие на лого
перенаправляет на список тасков), тогда всё появится

вероятнее всего, проблема в том, что при подтягивании данных о тасках из 
бд, реакт не видит, что что-то изменилось и не осуществляет повторный
рендеринг компонента

пока решение найти не получилось, но не хочется слишком сильно затягивать
с отправкой



Проблема 2

не до конца разобралась с докером, контейнеры создаются, запускаются,
но при отправке запроса с клиента на сервер появляется ошибка ERR_EMPTY_RESPONSE

опять-таки, решение пока не нашла, но не знаю, когда смогу разобраться
